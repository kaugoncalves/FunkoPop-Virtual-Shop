require('marko/node-require').install();
require('marko/express');

/* const express    = require('express');
// criando a aplicação NODE
const aplicacao  = express();

const path = require=('path');

aplicacao.use('/static',express.static('src/app/views'))
aplicacao.use(express.static(path.join(__dirname, 'views')));

aplicacao.use(express.urlencoded({
    extended: true
  }))
  aplicacao.use(express.static(__dirname + '../app/views'));





const bodyParser = require('body-parser');

const rotas = require('../app/rotas/rotas');
rotas(aplicacao);

module.exports = aplicacao; */